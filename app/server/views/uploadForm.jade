// preset form values if we receive a userdata object //
- user = typeof(udata) != 'undefined' ? udata : { }
// store the userId on the client side in a hidden input field //

#account-form-container
	form( method="post")#account-form.form-horizontal.well.span6
	
		h1
		p#sub1.subheading Please Upload Your Documents.
		hr
		fieldset
			#name-cg.control-group
				label.control-label(for='name-tf') File
				.controls
					input#name-tf.input-xlarge(type='file', name='input_file')
					input#name-tf.input-xlarge(type='text', name='userID',value=user._id)
					
			#email-cg.control-group
				label.control-label(for='email-tf') Comments
				.controls
					input#email-tf.input-xlarge(type='text', name='comments')
			#clist-cg.control-group
				label.control-label(for='country-list') Location
				.controls
					select#country-list(name='country')
						each item in countries
							option(selected=item.name == user.country)= item.name

			.form-actions
				button(type='submit')#account-form-btn1.btn
				